# Läuft bei jedem Push und jedem Pull Request.
#
# Die Stufen entsprechen den Gates aus der ROADMAP: erst die Tests beider
# Ausprägungen, dann Android Lint, dann der Bau -- und am Ende die
# beiden Tore: die Auslieferung bleibt netzfrei, und die Lizenzlage der
# ausgelieferten Abhängigkeiten hat sich nicht unbemerkt geändert. Schlägt irgendetwas
# fehl, ist der Lauf rot; ein "fast grün" gibt es nicht.
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  pruefen:
    name: Tests, Lint, Bau, Netzfreiheit
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - name: JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Tests (Auslieferung)
        run: ./gradlew testOfflineDebugUnitTest --console=plain

      - name: Tests (Forschung)
        run: ./gradlew testForschungDebugUnitTest --console=plain

      - name: Android Lint
        run: ./gradlew lintOfflineDebug --console=plain

      - name: Bau beider Ausprägungen
        run: ./gradlew assembleOfflineDebug assembleForschungDebug --console=plain

      - name: "Gate: Auslieferung bleibt netzfrei"
        run: ./gradlew pruefeNetzfreiheit --console=plain

      # Frühwarnung, keine Rechtsberatung: schlägt an, wenn eine
      # ausgelieferte Abhängigkeit andere Bedingungen mitbringt als bisher
      # oder in lizenzen.txt fehlt. Hier soll das auffallen -- nicht im
      # Store.
      - name: "Gate: Lizenzlage unverändert"
        run: ./gradlew pruefeLizenzen --console=plain

      - name: Testberichte sichern
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: testberichte
          path: app/build/reports/
          retention-days: 14
