<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Kein INTERNET-Recht: Nibra erkennt ausschliesslich auf dem
         Geraet (SpeechRecognizer.createOnDeviceSpeechRecognizer ab API 33,
         sonst SpeechRecognizer mit EXTRA_PREFER_OFFLINE). Siehe AUFTRAG.md,
         Nachtrag "Spracherkennung, Entscheidung". -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        Seit Android 11 sieht eine App andere Pakete nur, wenn sie den
        Bedarf erklärt. Ohne diesen Block blockiert das System die
        Verbindung zum Erkennungsdienst kommentarlos:

          AppsFilter: nibra -> com.google.android.as BLOCKED

        und beim Diktat kommt RECOGNIZER_BUSY, als wäre besetzt. Dabei
        durfte Nibra den Dienst schlicht nicht sehen. Am S23 Ultra gemessen;
        welche App den Dienst stellt (Google-TTS, System Intelligence),
        unterscheidet sich je Gerät, deshalb die Absicht statt eines
        Paketnamens.
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name="de.ithandwerkstuttgart.nibra.DiktatApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nibra"
        tools:targetApi="37">

        <activity
            android:name="de.ithandwerkstuttgart.nibra.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Nibra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="de.ithandwerkstuttgart.nibra.dienst.DiktatBedienungshilfenDienst"
            android:enabled="true"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/dictation_accessibility_service" />
        </service>

    </application>

</manifest>
